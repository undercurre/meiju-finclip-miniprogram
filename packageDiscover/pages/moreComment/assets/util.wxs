function discussPeopleHeader(item) {
  var avatar = item.userInfo.headPortrait
    ? item.userInfo.headPortrait
    : 'https://fcmms.midea.com/ccrm-sit/userHeadImg/defaultHeadImg.png'
  return avatar
}

module.exports = {
  discussPeopleHeader: discussPeopleHeader,
}
