function format(productDataObj, statusBarHeight) {
  var str = statusBarHeight + 6 + 30 + 10 + 16
  if (!productDataObj.iconUrl) {
    str = statusBarHeight + 6 + 30 + 10 + 50
  }
  return str
}
module.exports = {
  format: format,
}
