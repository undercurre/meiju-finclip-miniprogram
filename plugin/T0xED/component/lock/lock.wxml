<van-cell title="{{title}}" label="{{desc}}" custom-class="{{cellStyle}}" border="{{border}}">
  <view class="cell-img-wrap flex align_center justify_center" style="background:{{iconColor}};" slot="icon">
    <image src="{{images.lock}}" class="cell-img" />
  </view>
  <van-switch checked="{{lock}}" slot="right-icon" active-color="{{iconColor}}"
    bind:change="onSwitchChange" size="40rpx" disabled="{{lockDisable}}"/>
</van-cell>
