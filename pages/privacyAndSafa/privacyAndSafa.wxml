<view class="main-container">
    <mx-cell 
        wx:for="{{cellList}}" 
        wx:for-item="cellItem"
        wx:key="id"
        wx:for-index="idx" 
        title="{{cellItem.title}}" 
        data-item="{{cellItem}}"
        bind:click="jumpTargetPath" 
        is-link/>
</view>
